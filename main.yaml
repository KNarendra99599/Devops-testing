---
name: Reusable Workflow 1
on:
  workflow_dispatch:
  
  push:
    branches:
      - main
      
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: code checkout
        uses: actions/checkout@v4
        
      - name: Write something to log
        run: |
          echo "I am Reusable Workflow One on Main branch."
